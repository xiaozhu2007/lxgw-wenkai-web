{
  "version": "0.0.0",
  "name": "lxgw-wenkai-web-monorepo",
  "description": "monorepo for lxgw-wenkai-web",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "python -m update",
    "build:package": "python -m update --package",
    "release": "npx -y -p @qiwi/semrel-toolkit multi-semrel"
  }
}
